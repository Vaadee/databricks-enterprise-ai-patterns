resources:
  apps:
    background_task_app:
      name: ${var.app_name}-${bundle.target}
      description: "Async inference — background tasks, no external Lakeflow worker"
      source_code_path: ../app
      # No job resource reference needed — inference runs in-process
